{
  "name": "ionic-stencil-observables",
  "private": true,
  "version": "0.0.1",
  "description": "ionic-stencil-observables",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "one",
    "rxjs": "^6.5.2"
  },
  "devDependencies": {
    "@stencil/core": "1.0.0-beta.8"
  }
}
